;(function () {
    "use strict";
    angular
        .module('app')
        .controller('Settings', Settings);

    Settings.$inject = ['user'];

    function Settings (user) {

        var vm = this;

    }

})();
